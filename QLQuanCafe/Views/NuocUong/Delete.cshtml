@model Application.DTOs.NuocUongDto

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>
<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Product</h4>
    <hr/>
    <dl class="row">
       <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Id)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => Model.Id)</dd>
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.TenNU)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => Model.TenNU)</dd>
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.DonGia)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => Model.DonGia)</dd>
    </dl>

    <form method="POST">
        <input type="hidden" asp-route-id="@Model.Id"/>
        <input type="submit" value="Delete" class="btn btn-danger"/>
        <a asp-action="Index">Back to list</a>
    </form>
</div>